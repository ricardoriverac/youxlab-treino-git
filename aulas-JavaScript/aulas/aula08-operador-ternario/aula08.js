let numero1=10
resultado = (!(numero1%2) ? 'Par' : 'Impar') // quando nega ele inverte o resultado da operação fazendo ele pegar o 'par'
console.log(resultado)

let status = 'A'
resposta = (status == 'A' ? 'Ativo' : 'se Falso')
// teste lógico ? se verdadeiro : se falso