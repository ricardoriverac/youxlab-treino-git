

# ---

# ### 🔁 Estrutura de repetição `while`

# O **`while`** é uma estrutura de repetição (loop) usada em linguagens de programação para executar um bloco de código **enquanto uma condição for verdadeira**.

# #### 📌 Sintaxe básica (exemplo em Python):
# ```python
# while condição:
#     # bloco de código que será repetido
# ```

# #### ✅ Funcionamento:
# 1. A **condição é verificada**.
# 2. Se for **verdadeira (`True`)**, o código dentro do loop é executado.
# 3. Após a execução, a condição é verificada novamente.
# 4. O loop continua até que a condição se torne **falsa (`False`)**.

# #### ⚠️ Importante:
# - Se a condição **nunca for falsa**, o loop pode se tornar **infinito**.
# - É comum usar uma **variável de controle** que muda dentro do loop para garantir que ele pare.

# #### 💡 Exemplo:
# ```python
# contador = 0

# while contador < 5:
#     print("Contando:", contador)
#     contador += 1
# ```

# **Saída:**
# ```
# Contando: 0
# Contando: 1
# Contando: 2
# Contando: 3
# Contando: 4
# ```

# ---

# Se quiser, posso fazer um exemplo em outra linguagem também, tipo C, Java ou JavaScript. Só falar!